clase github
------------

gerenado tu Public Key

ssh-keygen

-agregarle un password
----------------------

cat ~/.ssh/id rsa.pub

primeros pasoso
git init

touche README

git add README

git commit -m "Tu primir commit"

git push origin @direction
----------------------------